import { extendTheme } from "@chakra-ui/react";

const colors = {
  primary: {
    light: "#F7F8FA",
    dark: "#252934",
  },
  secondary: {
    light: "#90cdf4",
    dark: "#1ca7d0",
  },
};

export default extendTheme({
  colors,
});
